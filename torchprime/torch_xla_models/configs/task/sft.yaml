# Task configuration for supervised fine-tuning
name: sft
global_batch_size: 16
max_steps: 10
export_checkpoint_path: export
optimizer:
  learning_rate: 4.e-5
  type: adafactor
lr_scheduler:
  type: linear
  warmup_steps: 10

